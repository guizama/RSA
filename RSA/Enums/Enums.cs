namespace RSA.Enums
{
    public class Enums
    {
        public enum KeyType
        {
            publicKey = 1,
            privateKey = 2
        }

        public enum LogType
        {
            INSERT,
            CONSULT
        }
    }
}
